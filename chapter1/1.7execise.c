#include <stdio.h>
//finally got it so here is the answer
main()
{
 int c;
 while(( c= getchar()) != EOF);
 putchar(c);	//this actually can be tested if you hit control^D
}		// I have to google it out
